This backend is original Clawmacs project work and follows the repository
license unless otherwise noted.

Implementation shape is informed by McCLIM's `Backends/Null` and
`Backends/CLX` backend protocols. Those upstream files are LGPL-2.1+ and are
not copied verbatim into this directory.

McCLIM examples coverage defaults to a local checkout at
`/home/tay/reference/external_src/McCLIM/Examples`, or `MCCLIM_SOURCE_ROOT`
when set.
